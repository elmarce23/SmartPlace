package com.example.smartplace;

public class API_Connector {
    private static final String url = "api_url";
    protected boolean res;

    protected boolean putEstado(int id){
        res = true ? true:false;
        return res;
    }
}
